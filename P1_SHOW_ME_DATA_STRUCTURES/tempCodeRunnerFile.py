  # inter = []
    # l1 = llist_1.get_values()
    # l2 = llist_2.get_values()
    # max_list_length = max(len(l1), len(l2))
    # max_list = l1 if (len(l1) == max_list_length) else l2
    # for i in range(len(max_list_length)):
    #     if